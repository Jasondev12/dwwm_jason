let variableLet = "globale";
let variableVar = "globale";

if(true) {
  
  let variableLet = "locale";
  let variableVar = "locale";
  
  console.log("let : " + variableLet);
  console.log("var : " + variableVar);
  
}

console.log("let : " + variableLet);
console.log("var : " + variableVar);